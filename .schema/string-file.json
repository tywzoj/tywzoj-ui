{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The string name, unique identifier for the string"
      },
      "value": {
        "type": "string",
        "description": "The string value. It can include placeholders like {0}, {1}, etc."
      },
      "description": {
        "type": "string",
        "description": "The string description. It explains the context and usage of the string. Helpful for translators."
      }
    },
    "required": ["name", "value", "description"],
    "additionalProperties": false
  }
}
